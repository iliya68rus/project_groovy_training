pipeline {
    agent any

    stages {
        stage('PrintSysTime') {
            steps {
//                 def branchName = env.BRANCH_NAME
//                 echo "branch name: ${branchName}"
//                 echo 'Pulling...' + env.BRANCH_NAME
                echo "Pulling2... ${branch}"
            }
        }
        stage('Hello') {
            steps {
                echo 'Hello World from git'
            }
        }
    }
}